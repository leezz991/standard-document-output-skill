---
name: standard-document-output
description: 使用保留的 Word 模板生成本单位标准格式文件，并在“公文模式”和“通用模式”之间选择。用户要求输出正式 Word、标准格式文件、单位公文、通知、请示、报告、函、纪要，或对外协作、交流、沟通、汇报、方案、说明材料时使用。公文模式采用 gongwen.docx；通用模式采用 reference.docx；保留各模板的页面、字体、标题层级、编号、缩进和行距，并在交付前渲染检查。
---

# 标准格式文件输出

先选择模式，再使用相应模板。模板资产是格式权威，不用通用设计预设覆盖它们。

## 模式选择

- **公文模式（official）**：单位内部正式公文，尤其是通知、请示、报告、函、纪要和制度性材料。使用 `assets/gongwen.docx`，并读取 `references/official-format.md`。
- **通用模式（general）**：对外协作、交流、沟通、汇报、方案、说明、参考资料等非正式公文材料。使用 `assets/reference.docx`，并读取 `references/general-format.md`。
- 用户明确指定模式时服从用户。
- 用户未指定时，根据用途自动选择，并在回复中简要说明选择结果；若两种模式都合理且选择会显著影响结果，先询问用户。

## 统一输入格式

先把内容整理成 UTF-8 Markdown：

- `#`：文档主标题。
- `> `：副标题，仅通用模式使用。
- `##` 至 `#####`：一至四级标题。
- 无标记段落：正文。空行只用于分段，不生成空白段落。

不要手工输入模板自动生成的层级编号。公文模式四级标题均由模板自动编号；通用模式保留模板原生标题和编号行为。

## 生成与检查

1. 完成内容；信息不足时保留清晰占位符，不虚构单位、文号、日期、联系人或责任人。
2. 运行 `python scripts/build_standard_doc.py --mode official input.md output.docx`，或将模式改为 `general`。
3. 不直接修改 `assets/gongwen.docx` 或 `assets/reference.docx`。
4. 用 Word 或 Documents 技能渲染结果，逐页检查乱码、溢出、孤行、异常空白、标题层级、自动编号、表格和页眉页脚。
5. 只在最新渲染结果无明显缺陷后交付。

## 修改既有文件

先判断原文件属于哪种模式。内容性修改尽量局部完成；若格式混乱或用户要求按标准重排，抽取内容、重建 Markdown 层级，再用对应模式生成新文件并保留原件。

## 未定义元素

模板没有规定表格、图片、附件说明或特殊落款时，优先服从用户要求。否则采用克制、可读、与所选模板一致的格式，并通过渲染复核；不得改变基础页面设置和字体体系。
